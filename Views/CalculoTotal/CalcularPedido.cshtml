@model float

@{
    ViewData["Title"] = "Total do Pedido";
}

<div class="container mt-4">
    <div class="card shadow-sm border-primary">
        <div class="card-header bg-primary text-white">
            <h3 class="card-title mb-0">Resultado do Cálculo (Strategy - Pedido)</h3>
        </div>
        <div class="card-body text-center py-5">
            <h5 class="text-muted mb-3">Valor Total Calculado:</h5>
            <p class="display-3 fw-bold text-success">R$ @Model.ToString("N2")</p>
        </div>
        <div class="card-footer bg-light">
            <a href="/" class="btn btn-secondary">Voltar</a>
        </div>
    </div>
</div>